version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build:
    executor:
      name: node/default
    steps:
       - run: echo build step
  test:
    executor:
      name: node/default
    steps:
      - run: echo test step

workflows:
    build-and-test:
      jobs:
        - build
        - test:
            requires:
              - build